[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "opencv-python"
version = "99.99.99"
description = "Dummy package: prevents ultralytics from pulling the GUI-linked opencv-python build. Real OpenCV comes from opencv-python-headless."
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
